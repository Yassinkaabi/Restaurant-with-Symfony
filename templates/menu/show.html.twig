{% extends 'dashboard.html.twig' %}

{% block title %}Menu{% endblock %}

{% block content %}
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Menu</th>
                <td>{{ menu.menu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_menu') }}">back to list</a>

    <a href="{{ path('app_menu_edit', {'id': menu.id}) }}">edit</a>

    {{ include('menu/_delete_form.html.twig') }}
{% endblock %}
