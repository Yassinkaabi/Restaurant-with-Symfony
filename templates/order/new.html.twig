{# templates/order/new.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create a New Order</h1>

    {{ form_start(form) }}
    {{ form_row(form.food) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
