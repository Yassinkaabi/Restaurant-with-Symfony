{{ form_start(form, {'attr': {'class': 'form'}}) }}

    {# {{ form_row(form.user, {'attr': {'class': 'form-control mb-3'}}) }} #}
    {{ form_row(form.content, {'attr': {'class': 'form-control mb-3'}}) }}
    {{ form_row(form.restaurant, {'attr': {'class': 'form-control mb-3'}}) }}
    {{ form_row(form.table, {'attr': {'class': 'form-control mb-3'}}) }}
    {{ form_row(form.reservationTime, {'attr': {'class': 'form-control mb-3'}}) }}
    {# {{ form_row(form.isVerified, {'attr': {'class': 'form-check-input'}}) }} #}

    <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
